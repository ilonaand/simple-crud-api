const Database = require("./database");

const database = new Database();

database.add('name', 15, hobbies = ['one', 'two']);

database.update('1', "ina", 18, ['1', '2', '6'])
console.log(database.findall());